<!doctype html>
<html lang="en">
    <head>
        <%- include('../partials/head'); %>
    </head>
    <body>
    <header>
        <%- include('../partials/header'); %>
    </header>
        <main>
            <div class="container mt-5">
                <h1 class="text-center mb-4">Menú</h1>
                <div class="container mt-5">
                    <h1 class="text-center mb-4 fw-bold text-primary">Nuestro Menú 🍽️</h1>
                    <div class="row">
                        <% if (menuItems && menuItems.length > 0) { %>
                            <% menuItems.forEach(item => { %>
                                <div class="col-lg-4 col-md-6">
                                    <div class="card shadow-lg mb-4 border-0">
                                        <div class="card-body text-center">
                                            <h5 class="card-title fw-bold text-dark"><%= item.name %></h5>
                                            <p class="card-text text-muted"><%= item.description || "Delicioso platillo" %></p>
                                            <p class="fw-bold text-success fs-4">$<%= item.price.toFixed(2) %></p>
                                            <button class="btn btn-primary w-100">Ordenar</button>
                                        </div>
                                    </div>
                                </div>
                            <% }) %>
                        <% } else { %>
                            <div class="col-12 text-center">
                                <p class="alert alert-warning">No hay elementos en el menú.</p>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </main>
    </body>

    <footer>
        <%- include('../partials/footer'); %>
    </footer>
</html>
